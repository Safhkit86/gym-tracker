# Copia questo file in .env.local per lo sviluppo locale (Vite lo legge da solo).
# Se assente, il client usa il default http://localhost:4000 (vedi src/api/client.ts).
VITE_API_BASE_URL=http://localhost:4000
